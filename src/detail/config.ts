export const prevBufCount = 3;
export const displayCount = 1;
export const nextBufCount = 3;
export const preCacheCount = 10;

export const operateClickableArea: {
  x: number;
  y: number;
} = {
  x: 0.5,
  y: 0.5,
};

export const checkMisClickOperateTime = 300;
